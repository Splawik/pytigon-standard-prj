% extends "school/element.html"

% load exfiltry
% load exsyntax

% comment:
    %% id_extra
        % if table_type == 'datatable' and not get:
            .AAAAAA
        
        .X1[{{table_type}}].X2[{{get}}].X3

        % row_actions:
            .field_list/subject_set,Subjects,fa fa-lg fa-plus/fa fa-list-ol        
        {{ block.super }}

    %% row_edit
        % form: 
            .parent:!, type:!, code, name, description
